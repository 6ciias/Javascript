function min(x, y) {
    if (x < y) {
      return x;
    } else {
      return y;
    }
  }
  
  console.log(min(10, 20)); // Console log the function and input your variables 

  // Here we have function min that takes 2 variables
// We make the arguemnt that if X is less than Y it will return X (the Min)
// If it's greater it will return Y

